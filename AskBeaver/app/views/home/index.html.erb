<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to 'Ask Beaver', home_index_path, :class=>"brand"%>
            <div class="nav-collapse collapse">
              <ul class="nav pull-right">
                <% if user_signed_in? %>
                <li><a href="#user"><%= current_user.email%></a><li>
                <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
                <% else %>
              <li><%= link_to "Sign in", new_user_session_path%></li>
            <% end %>
          </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>

<br>
<div class="container">
	<div class="hero-unit">
    <!-- Main hero unit for a primary marketing message or call to action -->
		<h1><%= @hola %></h1>
		<br>
		<p>Bienvenido al portal de preguntas y respuestas de castores curiosos</p>
		<br>

	<div class="pagination-centered">
	<% if user_signed_in? %>
		<% if current_user.user_type == "Admin" %>			
				<%= link_to "Administrar Preguntas", questions_path, :class=>"btn btn-primary btn-large", :style=>'color:#FFF'  %>			
				<%= link_to "Administrar Usuarios", users_path, :class=>"btn btn-primary btn-large", :style=>'color:#FFF; margin-left:100px'  %>
		<% else %>
			<%= link_to "Preguntas Personales" , personal_index_path , :class=>"btn btn-primary btn-large", :style=>'color:#FFF' %>
			<%= link_to "Preguntas", questions_path,  :class=>"btn btn-primary btn-large", :style=>'color:#FFF; margin-left:100px' %>
		<% end %>
	<% else %>
			<%= link_to "Preguntas", questions_path, :class=>"btn btn-primary btn-large", :style=>'color:#FFF' %>
	<% end %>
		</div>
	</div>
</div>