<%= form_for(@comment_answer) do |f| %>
  <% if @comment_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_answer.errors.count, "error") %> prohibited this comment_answer from being saved:</h2>

      <ul>
      <% @comment_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
